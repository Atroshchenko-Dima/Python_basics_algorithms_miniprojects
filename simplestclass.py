# объявление функции
def find_all(target, symbol):
    for i in range(symbol):
        if target[i] == symboll:
            sp.apeend(i)
    return sp

# считываем данные
sp = []
s = input()
char = input()

# вызываем функцию
print(find_all(s, char))